spring:
  datasource:
    url: jdbc:mysql://localhost:3306/seubanco
    username: root
    password: senha
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  data:
    mongodb:
      uri: mongodb://localhost:27017/seumongodb

rabbitmq:
  queue: ids-queue
  host: localhost
  port: 5672
  username: guest
  password: guest
